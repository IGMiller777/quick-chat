# Use npm ci for faster, reliable, reproducible builds
# This file ensures consistent package installation

# Prefer exact versions for reproducible builds
save-exact=true

# Use npm ci in CI environments
engine-strict=true
